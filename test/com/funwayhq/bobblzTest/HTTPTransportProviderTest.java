package com.funwayhq.bobblzTest;

import static org.junit.Assert.*;

import org.junit.Test;

import com.funwayhq.bobblz.HTTPTransportProvider;
import com.funwayhq.bobblz.User;

public class HTTPTransportProviderTest {
	
	/*@Test
	public void getAllTest() {
		HTTPTransportProvider transportProvider = new HTTPTransportProvider(User.class);
		String realValue = transportProvider.getAll();
		System.out.println("Real Value" + realValue);
	}
	
	@Test
	public void getOneTest() {
		HTTPTransportProvider transportProvider = new HTTPTransportProvider(User.class);
		String realValue = transportProvider.getOne();
		System.out.println("Real Value" + realValue);
	}
	
	@Test
	public void creteTest() {
		User user = new User();
		user.id = 1;
		user.name = "some name";
		user.age = 22;
		
		HTTPTransportProvider transportProvider = new HTTPTransportProvider(User.class);
		boolean realValue = transportProvider.create(user);
		assertTrue(realValue);
	}
	
	@Test
	public void saveTest() {
		User user = new User();
		user.id = 1;
		user.name = "some name";
		user.age = 22;
		
		HTTPTransportProvider transportProvider = new HTTPTransportProvider(User.class);
		boolean realValue = transportProvider.save(user);
		assertTrue(realValue);
	}
	
	@Test
	public void removeTest() {
		User user = new User();
		user.id = 1;
		user.name = "some name";
		user.age = 22;
		
		HTTPTransportProvider transportProvider = new HTTPTransportProvider(User.class);
		boolean realValue = transportProvider.save(user);
		assertTrue(realValue);
	}*/
}
